# 手工补数文件（仅本地/服务器保管）

本目录中的业务 Excel 默认被 Git 忽略，不得提交到 GitHub。仓库只保留本说明文件。

`天猫国际自营店数据补BI.xlsx` 用于修正“销售单查询”中销售渠道为“天猫国际自营”的金额。

更新时直接用新文件覆盖同名文件，保持以下约定：

- 工作表包含“吉客云”列。
- 实际金额放在工作表最后一列。
- 同一吉客云订单可以占多行，脚本按订单号汇总最后一列。
- 一个单元格若包含多个吉客云订单号，脚本会标记为歧义并跳过，不自动分摊金额。
